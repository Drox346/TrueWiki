# Configs

Shared project configuration lives here.

- `sources/` defines upstream wikis, APIs, and source authority metadata.
- `modpacks/` defines target modpacks, enabled sources, and local policy.

Local secrets and machine-specific settings should not be committed.

