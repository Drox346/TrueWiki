# truewiki-cli

Top-level command-line app for running TrueWiki workflows.

This is where glue code belongs once multiple packages need to be coordinated,
for example:

- fetch upstream source records
- extract and normalize records
- resolve modpack-specific truth
- compile local wiki output

No implementation lives here yet.

