# Apps

Runnable entry points live here.

Apps own workflow and orchestration. They can call into packages, read shared
configuration, and write workspace output, but reusable behavior should stay in
`packages/`.

